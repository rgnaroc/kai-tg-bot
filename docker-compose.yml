services:
  kai-bot:
    build: .
    container_name: kai-tg-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./bot:/app/bot       # код подтягивается с хоста
      - ./data:/app/data
      - ./.git:/app/.git:rw
    network_mode: host
